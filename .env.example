# Hostname passed to Twitch embeds as the `parent` query param.
# Set this to your production domain (no protocol), e.g. livesplit.app
# Falls back to window.location.hostname when unset.
VITE_TWITCH_PARENT_HOST=
